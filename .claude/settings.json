{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE\" == *.py ]]; then python -m py_compile \"$CLAUDE_FILE\" 2>&1 || true; fi"
          }
        ]
      }
    ]
  }
}